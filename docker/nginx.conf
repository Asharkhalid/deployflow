events {
    worker_connections 1024;
}

http {
    include       mime.types;
    default_type  application/octet-stream;

    sendfile        on;
    keepalive_timeout  65;

    # Include the dynamically managed upstream configuration.
    # In a real server setup, this file is generated by the deployment script
    # and placed in /etc/nginx/conf.d/upstream.conf to point to the active container.
    include /etc/nginx/conf.d/upstream.conf;

    server {
        listen 80;
        
        # Replace with your actual domain in production
        server_name _;

        location / {
            proxy_pass http://deployflow_upstream;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection keep-alive;
            proxy_set_header Host $host;
            proxy_cache_bypass $http_upgrade;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }
}
